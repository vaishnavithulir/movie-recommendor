---
description: Run the project
---

// turbo-all

1. Start the Streamlit server using the virtual environment:
```powershell
.\venv\Scripts\activate ; streamlit run app.py
```
2. Inform the user that the server has been safely started, and provide the exact Local URL: `http://localhost:8501`.
